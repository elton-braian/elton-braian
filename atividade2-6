#include <stdio.h>

int main() {
    int numero;
    printf("Digite um número inteiro: ");
    scanf("%d", &numero);

    if (numero >= 1 && numero <= 100) {
        printf("O número está entre 1 e 100.\n");
    } else {
        printf("O número não está entre 1 e 100.\n");
    }

    return 0;
}
